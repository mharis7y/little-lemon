// app/index.tsx
// This file is intentionally left minimal.
// The root _layout.tsx handles the redirect to the correct route
// based on the onboarding state read from AsyncStorage.
import { View } from 'react-native';

export default function Index() {
  return <View />;
}
